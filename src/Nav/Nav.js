import React, { Component } from "react";
import { Link } from "react-router-dom";

class Nav extends Component {
  render() {
    return (
      <div>
        <p>Here's the nav bar! maybe...</p>
        {/* <Link to="/home">Home</Link>
        <Link to="/profile">Profile</Link> */}
      </div>
    );
  }
}

export default Nav;
